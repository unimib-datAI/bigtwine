version: '2'
services:
    cronscheduler-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb cronscheduler-mongodb-config
    cronscheduler-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    cronscheduler-mongodb-config:
        image: mongo:4.0.2
        container_name: cronscheduler-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
